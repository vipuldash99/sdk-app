import { IJsonComponents } from "../../schemas/IJsonComponents";
import { IJsonSchema } from "../../module";
import { ApplicationProgrammer } from "../ApplicationProgrammer";
export declare const application_native: (options: ApplicationProgrammer.IOptions) => (components: IJsonComponents) => (name: string) => (nullable: boolean, attribute: IJsonSchema.IAttribute) => IJsonSchema.IReference;
